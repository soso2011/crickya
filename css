/* Set the default font and color for the whole page */
body {
  font-family: Arial, sans-serif;
  color: #333;
}

/* Style the navigation menu */
nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: #333;
  color: #fff;
}

nav a {
  color: #fff;
  text-decoration: none;
  padding: 1rem;
}

/* Style the search bar */
form {
  display: flex;
  align-items: center;
  margin: 2rem;
}

input[type="text"] {
  flex: 1;
  border: none;
  padding: 0.5rem;
  font-size: 1rem;
}

button[type="submit"] {
  border: none;
  background-color: #333;
  color: #fff;
  padding: 0.5rem 1rem;
  font-size: 1rem;
  cursor: pointer;
}

/* Style the image grid */
.image-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  margin: 2rem;
}

.image-grid img {
  width: 300px;
  height: 200px;
  object-fit: cover;
  margin: 1rem;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}

/* Style the footer */
footer {
  background-color: #333;
  color: #fff;
  text-align: center;
  padding: 1rem;
}

footer p {
  margin: 0;
}
